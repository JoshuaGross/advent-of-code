module.exports = function printConwayGrid (grid) {
  const dim = grid.keys().next().value.split(',').length;

  // TODO
  return str;/*

  let str = '';
  for (const z of range(minZ, maxZ)) {
    str += `z=${z}:\n`;
    for (const row of cube[z]) {
      str += row.map(val => val ? ACTIVE : INACTIVE).join('') + '\n';
    }
    str += '\n';
  }

  return str;*/
}
